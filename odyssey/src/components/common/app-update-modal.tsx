"use client";

/**
 * AppUpdateModal
 *
 * Odyssey uses a Live Hybrid Shell architecture:
 * All UI, planner, habit, and styling updates deploy via Vercel in 30 seconds
 * and update automatically over the air without requiring APK rebuilds or self-downloading.
 * Returning null completely prevents intrusive "Download APK" pop-ups.
 */
export function AppUpdateModal() {
  return null;
}
